import 'package:flutter/material.dart';

class ProjectPadding {
  static const buttonPadding1 = EdgeInsets.symmetric(horizontal: 110, vertical: 15);
  static const buttonPadding2 = EdgeInsets.symmetric(horizontal: 50, vertical: 15);
  static const paddingAllOne3 = EdgeInsets.all(24.0);
  static const paddingAllOne = EdgeInsets.all(16.0);
  static const paddingAllStandart = EdgeInsets.all(8.0);
  static const paddingAllStandart2 = EdgeInsets.all(4.0);
  static const paddingOnlyLeft = EdgeInsets.only(left: 10);
}
